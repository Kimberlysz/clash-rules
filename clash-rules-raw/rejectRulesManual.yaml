# general
 # build
  # build by Kim Xie / kimberlysz
  # build time start from: 2023-05-31

 # keywords
  # (cn): CN direct connect domains that's under "domain-list-community"

  # (deprecated): domains that's not affected or needed anymore

  # (revised): means domains that's included by v2fly-domain-rules but can directly connected in mainland China
    # how to process theose domains?
      # mark those domains

    # (@cn)

    # (@ir)

    # (@ru)

    # (@!cn)

  # (@!cn): domains that's under testing

 # format
  # rule clash (by using "rule-provider" configurations)
    # rules for clash configuration file
      # "rules:" will need to be replaced by "payload:"
        # payload:
        #     - '+.'
        #     - ''

  # rule shadowrocket
    # rules for shadoworcket app
      # "rules:" will need to be replaced by "[Rule]"

    # rules for domain-set and rule-set
      # "rules:" will need to be replaced by "payload:"

# website domain conflicts and mis-replacement
 # WARNING: The following domain names may be replaced incorrectly during the conversion process, or the following keyword might conflict with relevant websites
 # direct

 # proxy
   # aliyun (conflicts with)
    # .al

   # directenergy.com
    # '- DOMAIN-SUFFIX, \directenergy.com, \PROXY'
      # 1. keyword is '\direct'
      # 2. search "energy.com" and change it back to 'directenergy.com'

   # 'geosite:pingan bank' conflict with
    # 1. .cl
    # 2. .sd
    # 3. .orange

   # proxyium.com
    # '- DOMAIN-SUFFIX, \proxyium.com, \PROXY'
      # (1) keyword is '\proxy'
      # (2) search 'ium.com' and change it back to 'proxyium.com'

   # tld-!cn
   # tld-opennic

 # reject

rules:
  # c
   # category-ads-all
    # This file contains domains of all ads providers, including both the domains that serves ads, and the domains of providers themselves.
    # this is for crunchyroll.com

    - DOMAIN-SUFFIX, secure.adnxs.com, PROXY

  # g
   # google-ads (test)
    # these domains locates before reject rules ads
    # it's for crunchyroll.com
    - DOMAIN-SUFFIX, 2mdn.net, PROXY
    - DOMAIN-SUFFIX, beacons.gcp.gvt2.com, PROXY
    # - DOMAIN-SUFFIX, csi.gstatic.com, PROXY
    - DOMAIN-SUFFIX, doubleclick.net, PROXY
    # - DOMAIN-SUFFIX, googleadservices.com, PROXY
    - DOMAIN-SUFFIX, googlesyndication.com, PROXY
    # - DOMAIN-SUFFIX, googletagmanager.com, PROXY
    # - DOMAIN-SUFFIX, imasdk.googleapis.com, PROXY
    - DOMAIN-SUFFIX, redirector.gvt1.com, PROXY
    # - DOMAIN-SUFFIX, ssl.gstatic.com, PROXY


